import styles from "./Calendar.module.css";

//미션 시작일부터 종료일까지 각각 도전 성공 정보가 있어야 함.
//달력 모양 계산 로직 작성
function Calendar({}) {
  return (
    <div>
      <div>header</div>
      <div>7*5 grid line, independent days info.</div>
    </div>
  );
}

export default Calendar;
